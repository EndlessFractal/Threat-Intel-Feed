name: Update Feed

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"

jobs:
  update_hosts:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Python
        uses: actions/checkout@v5
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Set git config
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@github.com"

      - name: Run Parser
        run: python FeedMerger.py

      - name: Check for feed updates
        id: check_changes
        run: |
          if diff -q \
            <(git show HEAD:feed.xml | grep -v -E '<(pubDate|lastBuildDate)>') \
            <(cat feed.xml | grep -v -E '<(pubDate|lastBuildDate)>') > /dev/null; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "No meaningful changes to commit."
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Meaningful changes detected in feed.xml"
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git add feed.xml
          git commit -am "Update RSS feed"
          git push
